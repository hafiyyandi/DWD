<html>
  <head>
    <title>Some Template</title>
    <script type="text/javascript">
      function setup() {
        var datafromserver = "<%=thedata%>";
        //console.log(datafromserver[0]._id);
        //alert(datafromserver);
      }
      window.addEventListener('load', setup);
    </script>

  </head>
  <body>

    <% for (var i = 0; i < thedata.length; i++) { %>
      <% var videoIDs = []; %>
      <% var newItem = thedata[i].liveVideoID;%>
      <% videoIDs.indexOf(newItem) === -1 ? videoIDs.push(newItem) : console.log("This item already exists”); %>
    <% } %>
    <% for (var j = 0; j < videoIDs.length; j++) { %>
      <a href="/one?_id=<%= videoIDs[j] %>"><%= videoIDs[j] %> :
    <%= videoIDs[j] %> </a><br />
    <!--JSON.stringify(thedata[i])-->
    <% } %>

  </body>
</html>